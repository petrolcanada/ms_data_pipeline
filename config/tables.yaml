# Snowflake Table Sync Configuration

# Shared values — define once, reference with *alias
peer_filter: &peer_filter "WHERE _ID IN (SELECT mstarid FROM CIGAM_SBX_PL.DNA_ANALYTICS_INSIGHTS_INVESTMENT_PRODUCT.V_MSTAR_PEERS_FCLASS_ETF_ID_CA_OPENEND)"

# Auto-generated from merge_keys at export time:
#   QUALIFY ROW_NUMBER() OVER (PARTITION BY {merge_keys} ORDER BY {qualify_order_by|_TIMESTAMPTO} DESC) = 1

sf_defaults: &sf_defaults
  database: "CIGAM_PRD_RL"
  schema: "MORNINGSTAR_MAIN"

tables:
  # --- Tables with merge on _ID + _TIMESTAMPFROM ---

  - name: "FUND_SHARE_CLASS_BASIC_INFO_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "FUND_SHARE_CLASS_BASIC_INFO_CA_OPENEND"
      source_query: |
        WITH month_ends AS (
            SELECT DISTINCT MONTHENDDATE
            FROM CIGAM_PRD_RL.MORNINGSTAR_MAIN.MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND
        )
        SELECT src.*, m.MONTHENDDATE
        FROM {table} src
        INNER JOIN month_ends m
            ON src._TIMESTAMPFROM < m.MONTHENDDATE
            AND m.MONTHENDDATE <= src._TIMESTAMPTO
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY src._ID, m.MONTHENDDATE
            ORDER BY src._TIMESTAMPFROM DESC, src._TIMESTAMPTO DESC
        ) = 1
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "FUND_SHARE_CLASS_BASIC_INFO_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "FUND_ATTRIBUTES_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "FUND_ATTRIBUTES_CA_OPENEND"
      source_query: |
        WITH month_ends AS (
            SELECT DISTINCT MONTHENDDATE
            FROM CIGAM_PRD_RL.MORNINGSTAR_MAIN.MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND
        )
        SELECT src.*, m.MONTHENDDATE
        FROM {table} src
        INNER JOIN month_ends m
            ON src._TIMESTAMPFROM < m.MONTHENDDATE
            AND m.MONTHENDDATE <= src._TIMESTAMPTO
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY src._ID, m.MONTHENDDATE
            ORDER BY src._TIMESTAMPFROM DESC, src._TIMESTAMPTO DESC
        ) = 1
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "FUND_ATTRIBUTES_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "FUND_MANAGER_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "FUND_MANAGER_CA_OPENEND"
      source_query: |
        WITH month_ends AS (
            SELECT DISTINCT MONTHENDDATE
            FROM CIGAM_PRD_RL.MORNINGSTAR_MAIN.MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND
        )
        SELECT src.*, m.MONTHENDDATE
        FROM {table} src
        INNER JOIN month_ends m
            ON src._TIMESTAMPFROM < m.MONTHENDDATE
            AND m.MONTHENDDATE <= src._TIMESTAMPTO
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY src._ID, m.MONTHENDDATE
            ORDER BY src._TIMESTAMPFROM DESC, src._TIMESTAMPTO DESC
        ) = 1
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "FUND_MANAGER_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "PROSPECTUS_FEES_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "PROSPECTUS_FEES_CA_OPENEND"
      source_query: |
        WITH month_ends AS (
            SELECT DISTINCT MONTHENDDATE
            FROM CIGAM_PRD_RL.MORNINGSTAR_MAIN.MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND
        )
        SELECT src.*, m.MONTHENDDATE
        FROM {table} src
        INNER JOIN month_ends m
            ON src.PROSPECTUSDATE <= m.MONTHENDDATE
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY src._ID, m.MONTHENDDATE
            ORDER BY src.PROSPECTUSDATE DESC, src._TIMESTAMPTO DESC
        ) = 1
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "PROSPECTUS_FEES_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  # --- Tables with dedicated date key ---

  - name: "MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND"
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "FUND_FLOW_DETAILS_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "FUND_FLOW_DETAILS_CA_OPENEND"
      source_query: |
        SELECT *, ESTFUNDLEVELNETFLOWDATEMOEND AS MONTHENDDATE
        FROM {table}
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "FUND_FLOW_DETAILS_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "MORNINGSTAR_RATING_EXTENDED_PERFORMANCE_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "MORNINGSTAR_RATING_EXTENDED_PERFORMANCE_CA_OPENEND"
      source_query: |
        SELECT *, RATINGDATE AS MONTHENDDATE
        FROM {table}
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "MORNINGSTAR_RATING_EXTENDED_PERFORMANCE_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "MONTH_END_TRAILING_TOTAL_RETURN_PERCENTILE_AND_ABSOLUTE_RANKS_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "MONTH_END_TRAILING_TOTAL_RETURN_PERCENTILE_AND_ABSOLUTE_RANKS_CA_OPENEND"
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "MONTH_END_TRAILING_TOTAL_RETURN_PERCENTILE_AND_ABSOLUTE_RANKS_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "RELATIVE_RISK_MEASURE_PROSPECTUS_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "RELATIVE_RISK_MEASURE_PROSPECTUS_CA_OPENEND"
      source_query: |
        SELECT *, ENDDATE AS MONTHENDDATE
        FROM {table}
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "RELATIVE_RISK_MEASURE_PROSPECTUS_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "FUND_LEVEL_NET_ASSETS_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "FUND_LEVEL_NET_ASSETS_CA_OPENEND"
      source_query: |
        WITH month_ends AS (
            SELECT DISTINCT MONTHENDDATE
            FROM CIGAM_PRD_RL.MORNINGSTAR_MAIN.MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND
        )
        SELECT src.*, m.MONTHENDDATE
        FROM {table} src
        INNER JOIN month_ends m
            ON src.NETASSETSDATE <= m.MONTHENDDATE
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY src._ID, m.MONTHENDDATE
            ORDER BY src.NETASSETSDATE DESC, src._TIMESTAMPTO DESC
        ) = 1
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "FUND_LEVEL_NET_ASSETS_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "RISK_MEASURE_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "RISK_MEASURE_CA_OPENEND"
      source_query: |
        SELECT *, ENDDATE AS MONTHENDDATE
        FROM {table}
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "RISK_MEASURE_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "ANNUAL_REPORT_FEES_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "ANNUAL_REPORT_FEES_CA_OPENEND"
      source_query: |
        WITH month_ends AS (
            SELECT DISTINCT MONTHENDDATE
            FROM CIGAM_PRD_RL.MORNINGSTAR_MAIN.MONTH_END_TRAILING_TOTAL_RETURNS_CA_OPENEND
        )
        SELECT src.*, m.MONTHENDDATE
        FROM {table} src
        INNER JOIN month_ends m
            ON src.ANNUALREPORTDATE <= m.MONTHENDDATE
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY src._ID, m.MONTHENDDATE
            ORDER BY src.ANNUALREPORTDATE DESC, src._TIMESTAMPTO DESC
        ) = 1
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "ANNUAL_REPORT_FEES_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "MORNINGSTAR_RATING_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "MORNINGSTAR_RATING_CA_OPENEND"
      source_query: |
        SELECT *, RATINGDATE AS MONTHENDDATE
        FROM {table}
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "MORNINGSTAR_RATING_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  - name: "FEE_LEVELS_CA_OPENEND"
    sync_mode: "upsert"
    merge_keys: ["_ID", "MONTHENDDATE"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "FEE_LEVELS_CA_OPENEND"
      source_query: |
        SELECT *, FEELEVELDATE AS MONTHENDDATE
        FROM {table}
      filter:
        - *peer_filter
    postgres:
      schema: "ms"
      table: "FEE_LEVELS_CA_OPENEND"
      indexes:
        - "_ID"
        - "MONTHENDDATE"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"

  # --- Category-level returns (no peer filter) ---

  - name: "CATEGORY_MONTHLY_NAV_TRAILING_PERFORMANCE_RETURNS"
    sync_mode: "upsert"
    merge_keys: ["CategoryCode", "DayEndDate"]
    watermark_column: "_TIMESTAMPFROM"
    snowflake:
      <<: *sf_defaults
      table: "CATEGORY_MONTHLY_NAV_TRAILING_PERFORMANCE_RETURNS"
      source_query: |
        SELECT
            RETURNDETAIL:CategoryCode::STRING      AS CategoryCode,
            RETURNDETAIL:CategoryName::STRING      AS CategoryName,
            RETURNDETAIL:DayEndDate::DATE          AS DayEndDate,
            RETURNDETAIL:Return1Mth::FLOAT         AS Return1Mth,
            RETURNDETAIL:Return3Mth::FLOAT         AS Return3Mth,
            RETURNDETAIL:Return6Mth::FLOAT         AS Return6Mth,
            RETURNDETAIL:Return9Mth::FLOAT         AS Return9Mth,
            RETURNDETAIL:ReturnYTD::FLOAT          AS ReturnYTD,
            RETURNDETAIL:Return1Yr::FLOAT          AS Return1Yr,
            RETURNDETAIL:Return2Yr::FLOAT          AS Return2Yr,
            RETURNDETAIL:Return3Yr::FLOAT          AS Return3Yr,
            RETURNDETAIL:Return4Yr::FLOAT          AS Return4Yr,
            RETURNDETAIL:Return5Yr::FLOAT          AS Return5Yr,
            RETURNDETAIL:Return6Yr::FLOAT          AS Return6Yr,
            RETURNDETAIL:Return7Yr::FLOAT          AS Return7Yr,
            RETURNDETAIL:Return8Yr::FLOAT          AS Return8Yr,
            RETURNDETAIL:Return9Yr::FLOAT          AS Return9Yr,
            RETURNDETAIL:Return10Yr::FLOAT         AS Return10Yr,
            RETURNDETAIL:Return15Yr::FLOAT         AS Return15Yr,
            RETURNDETAIL:Return20Yr::FLOAT         AS Return20Yr,
            REGION,
            STATUS_CODE,
            STATUS_MESSAGE,
            _NAME,
            _HASHKEY,
            _RUNID,
            _TIMESTAMPFROM,
            _TIMESTAMPTO,
            FAULT_FAULTSTRING,
            FAULT_DETAIL_ERRORCODE
        FROM {table}
    postgres:
      schema: "ms"
      table: "CATEGORY_MONTHLY_NAV_TRAILING_PERFORMANCE_RETURNS"
      indexes:
        - "CategoryCode"
        - "DayEndDate"
        - "_TIMESTAMPFROM"
        - "_TIMESTAMPTO"
